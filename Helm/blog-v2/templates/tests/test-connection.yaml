apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blog-v2.fullname" . }}-test-connection"
  labels:
    {{- include "blog-v2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36.1  
      command: ['wget']
      args: ['{{ include "blog-v2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
